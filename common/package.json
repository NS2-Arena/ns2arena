{
  "name": "@ns2arena/common",
  "version": "0.0.0",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "prepare": "tsc && esbuild --platform=node --format=cjs --minify-whitespace --minify-syntax --allow-overwrite --outdir=dist dist/*.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^24.9.2",
    "esbuild": "^0.25.12",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@types/aws-lambda": "^8.10.159",
    "aws-lambda": "^1.0.7"
  }
}
